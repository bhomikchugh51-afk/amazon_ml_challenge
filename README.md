# amazon_ml_challenge
Built a product-attribute extraction pipeline (weight, voltage, volume) from images for Amazon ML Challenge 2024. Established OCR+regex baseline, then designed a Qwen2-VL vision-language pipeline with structured JSON output and LoRA fine-tuning setup via PEFT for parameter-efficient adaptation.
